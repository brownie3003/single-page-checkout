<div class="row">
    <div class="col-md-12">
        <h2>Cart</h2>
    </div>
</div>

{{!-- order items section  --}}
<div class="row">
    <div class="col-md-12">
        {{#each order.items as |item|}}
            <div class="row">
                <div class="col-xs-12">
                    <div class="panel panel-default">
                        <div class="panel-body">
                            {{item-details item=item}}
                        </div>
                    </div>
                </div>
            </div>
        {{/each}}
    </div>
</div>

{{!-- order-total  --}}
<div class="row">
    <div class="col-md-12">
        <div class="panel panel-default">
            <div class="panel-body">
                {{!-- We are not yielding the shipping options component, so we need to 
                pass in the setShippingOption & setShippingCountry actions.  --}}
                {{#order-total 
                    order=order
                    setShippingOption="setShippingOption"
                    setShippingCountry="setShippingCountry"
                }}
                    {{mini-promo-code applyDiscount="applyDiscount" discount=order.discount}}
                {{/order-total}}
            </div>
        </div>
    </div>
</div>
<div class="row">
    <div class="col-md-12">
        <div class="panel panel-default">
            <div class="panel-body">
                {{#if order.user}}
                    {{#link-to 'order.shipping-options'}}
                        <button type="button" name="button" class="btn btn-block btn-moo">Checkout</button>
                    {{/link-to}}
                {{else}}
                    {{#link-to 'order.account.sign-in'}}
                        <button type="button" name="button" class="btn btn-block btn-moo">Checkout</button>
                    {{/link-to}}
                {{/if}}
            </div>
        </div>
    </div>
</div>
