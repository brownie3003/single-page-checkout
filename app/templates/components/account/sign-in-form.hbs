<div class="col-xs-12">
    <form class="form-group">
        <div class="form-group">
            <label>Email</label>
            {{validated-input type="text" placeHolder="Email"}}
        </div>
        <div class="form-group">
            <label>Password</label>
            {{validated-input type="text" placeHolder="Password"}}
        </div>
        <button type="submit" class="btn btn-moo btn-block">Sign in</button>
    </form>
</div>

{{yield}}
