<div class="col-xs-12">
    <form class="form-group" action="index.html" method="post">
        {{validated-input type="text" placeHolder="Email"}}
        {{validated-input type="text" placeHolder="Password"}}
        {{#link-to 'order.shipping-address'}}<button type="submit" class="btn btn-moo btn-block">Sign in</button>{{/link-to}}
    </form>
</div>

{{yield}}
